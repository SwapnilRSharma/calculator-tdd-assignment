{
  "name": "calculator-assignment",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest --watch-all --verbose"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  },
  "dependencies": {
    "jest": "^29.7.0"
  }
}
